# NOTE: copytruncate leaves a sparse file until the writer reopens its fd
# (claude remote-control keeps its stdout fd open for the lifetime of the
# session). Accepted quirk — see plan
# docs/superpowers/plans/2026-04-18-persistent-agent-reliability.md Task 3.

# session-willikins.log is the heavy transcript from `claude remote-control`
# (observed 359 MB pre-rotation). The operational logs below use smaller
# thresholds. Do NOT widen this to `session-*.log` — that glob also matches
# `session-manager.log`, which is in the second block, and logrotate errors
# on the resulting duplicate entry.
__AGENT_HOME__/.willikins-agent/session-willikins.log {
    size 50M
    rotate 5
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
    dateext
    dateformat -%Y%m%d-%s
}

__AGENT_HOME__/.willikins-agent/vk-bridge.log
__AGENT_HOME__/.willikins-agent/session-manager.log
__AGENT_HOME__/.willikins-agent/audit.jsonl
{
    size 20M
    rotate 3
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
